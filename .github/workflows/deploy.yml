name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to Railway
        run: |
          echo "🚀 Deploy to Railway using the Railway CLI or Railway GitHub integration"
          echo "Visit: https://railway.app/new?repo=https://github.com/Hazem-Hamd/LoginApiMysql"
